# Smart Expense Tracker

A smart AI-powered finance management system that helps users track, analyze, and control daily spending, especially for cash-based transactions.

This workspace contains a local copy of the project with frontend and backend folders.

Remote repository: https://github.com/Nitika-Mandal/Smart-Expense-Tracker

Structure:
- `backend/` - Node backend
- `frontend/` - Vite React frontend

Local notes:
- I initialized a local git repository and added `.gitignore`.
- To push to the remote, you may need to authenticate with GitHub (token or credentials).

